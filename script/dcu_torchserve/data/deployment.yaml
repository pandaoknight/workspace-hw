apiVersion: apps/v1
kind: Deployment
metadata:
  name: ts-def
  labels:
    app: ts-def
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ts-def
  template:
    metadata:
      labels:
        app: ts-def
    spec:
      containers:
        - name: torchserve
          image: pytorch/torchserve:latest-cpu
          ports:
            - containerPort: 8080
            - containerPort: 8081
          volumeMounts:
          - name: model-store
            mountPath: /home/model-server/model-store
      volumes:
        - name: model-store
          hostPath:
            path: /home/szl1160710/script/dcu_torchserve/data/model-store
